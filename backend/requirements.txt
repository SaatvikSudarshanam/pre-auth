fastapi==0.111.0
uvicorn[standard]==0.30.1
SQLAlchemy==2.0.31
pydantic==2.8.2
# EmailStr (schemas.py) needs email-validator; plain fastapi (no [standard] extra)
# does NOT pull it in, so pin it explicitly or the container crashes on startup.
email-validator==2.2.0
python-multipart==0.0.9
pypdf==4.2.0
passlib==1.7.4
PyJWT==2.8.0
httpx==0.27.0
python-dotenv==1.0.1
twilio==9.4.0
# OCR (images + scanned PDFs). RapidOCR bundles ONNX models; PyMuPDF rasterizes PDFs.
rapidocr-onnxruntime==1.4.4
pymupdf==1.28.0
Pillow==12.3.0
